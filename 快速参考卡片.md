# AI视觉质检系统 - 快速参考卡片

## 🚀 快速启动

```bash
# 1. 启动后端
cd edge && python main.py

# 2. 启动前端
cd frontend && npm run dev

# 3. 访问系统
http://localhost:5173

# 4. API 文档
http://localhost:8000/api/v1/docs
```

---

## 📡 API 端点速查

### 摄像头管理
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/cameras` | GET | 获取摄像头列表 |
| `/api/v1/inspection/start` | POST | 开始检测 |
| `/api/v1/inspection/stop` | POST | 停止检测 |

### 设备监控
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/device-status` | GET | 设备状态 |
| `/api/v1/system-health` | GET | 系统健康 |
| `/api/v1/performance-metrics` | GET | 性能指标 |

### 配置管理
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/config` | GET | 获取配置 |
| `/api/v1/config` | PUT | 更新配置 |
| `/api/v1/config/reset` | POST | 重置配置 |

### 模型管理
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/model-versions` | GET | 模型版本 |
| `/api/v1/model-versions/switch` | POST | 切换模型 |

### 其他
| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/health` | GET | 健康检查 |
| `/api/v1/detection-results` | GET | 检测结果 |
| `/ws` | WebSocket | 实时通信 |

---

## 💻 API 调用示例

### cURL 示例

```bash
# 健康检查
curl http://localhost:8000/api/v1/health

# 获取摄像头列表
curl http://localhost:8000/api/v1/cameras \
  -H "X-API-Key: your-api-key"

# 开始检测
curl -X POST http://localhost:8000/api/v1/inspection/start \
  -H "Content-Type: application/json" \
  -H "X-API-Key: your-api-key" \
  -d '{"camera_ids": ["camera_001"]}'

# 获取设备状态
curl http://localhost:8000/api/v1/device-status \
  -H "X-API-Key: your-api-key"
```

### JavaScript 示例

```javascript
// 获取摄像头列表
const cameras = await fetch('/api/v1/cameras', {
  headers: { 'X-API-Key': 'your-api-key' }
}).then(res => res.json())

// WebSocket 连接
const ws = new WebSocket('ws://localhost:8000/ws')
ws.onmessage = (event) => {
  const data = JSON.parse(event.data)
  console.log('收到消息:', data)
}
```

### Python 示例

```python
import requests

# 获取设备状态
response = requests.get(
    'http://localhost:8000/api/v1/device-status',
    headers={'X-API-Key': 'your-api-key'}
)
status = response.json()

# WebSocket 连接
import websocket
ws = websocket.WebSocketApp('ws://localhost:8000/ws')
ws.run_forever()
```

---

## 🔧 常用命令

### 后端命令

```bash
# 启动服务
cd edge && python main.py

# 运行测试
python tests\system_health_monitor.py --duration 60
python tests\test_system_reliability.py --duration 30

# 查看日志
tail -f edge/logs/app.log

# 清理缓存
rm -rf edge/__pycache__
find . -name "*.pyc" -delete
```

### 前端命令

```bash
# 开发模式
cd frontend && npm run dev

# 构建生产版本
npm run build

# 运行测试
npm run test

# 清理依赖
rm -rf node_modules package-lock.json
npm install
```

### 测试命令

```bash
# 集成测试
test-integration.bat

# 可靠性测试
test-reliability.bat

# 健康监控
python tests\system_health_monitor.py

# 可靠性测试
python tests\test_system_reliability.py
```

---

## 📊 阈值参考

### 健康状态阈值
| 指标 | 正常 | 警告 | 严重 |
|------|------|------|------|
| CPU 使用率 | < 70% | 70-85% | > 85% |
| 内存使用率 | < 80% | 80-90% | > 90% |
| 磁盘使用率 | < 80% | 80-90% | > 90% |
| FPS | > 20 | 15-20 | < 15 |

### 检测参数
| 参数 | 推荐值 | 说明 |
|------|--------|------|
| confidence_threshold | 0.5 | 置信度阈值 |
| iou_threshold | 0.45 | IOU 阈值 |
| max_detection_per_frame | 10 | 最大检测数 |

---

## 🐛 故障排查速查

### 问题诊断流程

```
1. 检查服务是否运行
   curl http://localhost:8000/api/v1/health

2. 检查 WebSocket 连接
   浏览器控制台查看 Network 标签

3. 查看日志
   tail -f edge/logs/app.log

4. 检查资源使用
   python tests\system_health_monitor.py
```

### 常见问题速查

| 问题 | 检查项 | 解决方案 |
|------|--------|----------|
| 后端无法启动 | Python 版本、依赖 | `pip install -r requirements.txt` |
| 前端无法连接 | API URL、CORS | 检查 `.env.development` |
| WebSocket 断开 | 防火墙、端口 | 允许 8000 端口 |
| 摄像头无法打开 | 占用、ID | 关闭其他程序、修改 source |
| 内存过高 | 缓存、GC | 调整 cache.max_size |

---

## 📦 配置速查

### config.yaml 关键配置

```yaml
# 服务器
server:
  host: 0.0.0.0
  port: 8000

# API 密钥
api_key: your-api-key-here

# 摄像头
cameras:
  - id: camera_001
    source: 0  # USB 摄像头
    enabled: true

# 检测参数
detection:
  confidence_threshold: 0.5
  iou_threshold: 0.45

# 缓存
cache:
  enabled: true
  max_size: 2000
  ttl: 600
```

### 环境变量

```bash
# 前端 .env.development
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws

# 后端（可选）
API_KEY=your-api-key
LOG_LEVEL=INFO
```

---

## 📈 性能指标参考

### 正常范围
| 指标 | 正常值 | 说明 |
|------|--------|------|
| API 响应时间 | < 50ms | RESTful API |
| WebSocket 延迟 | < 10ms | 实时推送 |
| 首次加载时间 | < 2s | 前端页面 |
| FPS | 20-30 | 检测帧率 |
| CPU 使用率 | < 60% | 空闲时 |
| 内存使用率 | < 70% | 空闲时 |

### 告警级别
| 级别 | 颜色 | 说明 |
|------|------|------|
| healthy | 绿色 | 正常运行 |
| warning | 黄色 | 需要关注 |
| critical | 红色 | 立即处理 |

---

## 🔐 安全建议

### API 密钥管理
```bash
# 生成随机 API 密钥
python -c "import secrets; print(secrets.token_urlsafe(32))"

# 在请求头中使用
-X-API-Key: your-api-key

# 定期更换（建议每月）
```

### CORS 配置
```python
# edge/src/api_server.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],  # 限制来源
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

## 📞 技术支持

### 有用的链接
- **API 文档**: http://localhost:8000/api/v1/docs
- **前端界面**: http://localhost:5173
- **项目文档**: `docs/` 目录
- **测试报告**: `tests/*.json`

### 日志位置
- **后端日志**: `edge/logs/app.log`
- **前端日志**: 浏览器控制台
- **测试日志**: `tests/*.log`

---

## 🎯 最佳实践

### 开发习惯
- ✅ 使用虚拟环境
- ✅ 定期备份配置
- ✅ 运行测试验证
- ✅ 监控系统健康
- ✅ 清理临时文件

### 部署建议
- ✅ 使用反向代理（Nginx）
- ✅ 启用 HTTPS
- ✅ 配置防火墙
- ✅ 定期更新依赖
- ✅ 监控资源使用

---

**版本**: 1.0  
**更新日期**: 2026-03-23  
**适用版本**: v1.0.0
