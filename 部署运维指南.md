# AI 视觉质检系统 - 部署与运维指南

## 📋 目录

- [系统要求](#系统要求)
- [安装步骤](#安装步骤)
- [配置说明](#配置说明)
- [启动服务](#启动服务)
- [运行测试](#运行测试)
- [故障排查](#故障排查)
- [性能优化](#性能优化)
- [备份恢复](#备份恢复)

---

## 系统要求

### 硬件要求
- **CPU**: Intel Core i5 或更高（推荐 i7）
- **内存**: 8GB 或更高（推荐 16GB）
- **显卡**: NVIDIA GTX 1060 或更高（支持 CUDA）
- **存储**: 至少 50GB 可用空间
- **摄像头**: USB 摄像头或 IP 摄像头

### 软件要求
- **操作系统**: Windows 10/11 或 Linux
- **Python**: 3.8 或更高版本
- **Node.js**: 16.x 或更高版本
- **CUDA**: 11.0 或更高版本（如需 GPU 加速）

---

## 安装步骤

### 1. 克隆项目
```bash
git clone <repository-url>
cd AISJZJRJT
```

### 2. 安装后端依赖

```bash
cd edge

# 创建虚拟环境（推荐）
python -m venv venv
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt

# 如果需要 GPU 支持
pip uninstall ultralytics
pip install ultralytics[gpu]
```

### 3. 安装前端依赖

```bash
cd ../frontend

# 安装 Node.js 依赖
npm install

# 如果使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install
```

### 4. 下载模型文件

```bash
# 自动下载 YOLOv8n 模型
python -c "from ultralytics import YOLO; YOLO('yolov8n.pt')"

# 或手动下载到以下位置：
# - edge/yolov8n.pt
# - model/yolov8n.pt
```

---

## 配置说明

### 后端配置

**文件位置**: `edge/config/config.yaml`

```yaml
# 服务器配置
server:
  host: 0.0.0.0
  port: 8000
  
# API 密钥
api_key: your-api-key-here

# 摄像头配置
cameras:
  - id: camera_001
    name: 主摄像头
    source: 0  # USB 摄像头 ID
    enabled: true
    
  - id: camera_002
    name: IP 摄像头
    source: rtsp://192.168.1.100:554/stream
    enabled: false

# 检测配置
detection:
  confidence_threshold: 0.5
  iou_threshold: 0.45
  max_detection_per_frame: 10
  
# MQTT 配置（可选）
mqtt:
  enabled: false
  broker: mqtt.example.com
  port: 1883
  username: user
  password: password
```

### 前端配置

**开发环境**: `frontend/.env.development`
```
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_WS_URL=ws://localhost:8000/ws
```

**生产环境**: `frontend/.env.production`
```
VITE_API_BASE_URL=https://your-domain.com/api/v1
VITE_WS_URL=wss://your-domain.com/ws
```

---

## 启动服务

### 方式一：使用批处理文件（推荐）

```batch
# Windows
launch.bat
```

### 方式二：手动启动

#### 1. 启动后端服务

```bash
cd edge
python main.py
```

成功标志：
```
API 服务器初始化成功，监听地址：0.0.0.0:8000
WebSocket 连接管理器已初始化
系统优化服务启动成功
摄像头管理器已初始化
模型加载成功：yolov8n.pt
```

#### 2. 启动前端服务

```bash
cd frontend
npm run dev
```

成功标志：
```
VITE v5.3.1  ready in 1234 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

#### 3. 访问系统

打开浏览器访问：http://localhost:5173

---

## 运行测试

### 前后端集成测试

```batch
test-integration.bat
```

选择测试模式：
- 1 - 快速测试（约 1 分钟）
- 2 - 标准测试（约 3 分钟）
- 3 - 完整测试（约 5 分钟）

### 可靠性测试

```batch
test-reliability.bat
```

选择测试模式：
- 1 - 快速测试（15 秒/项）
- 2 - 标准测试（30 秒/项）
- 3 - 完整测试（60 秒/项）
- 4 - 自定义测试
- 5 - 仅健康监控

### 手动运行测试

```bash
# 健康监控
python tests\system_health_monitor.py --duration 60

# 可靠性测试
python tests\test_system_reliability.py --duration 30
```

---

## 故障排查

### 问题 1: 后端服务无法启动

**症状**: 启动时报错或立即退出

**解决方案**:
```bash
# 检查 Python 版本
python --version  # 应该 >= 3.8

# 检查依赖是否完整
pip list | findstr "fastapi uvicorn ultralytics"

# 重新安装依赖
pip install -r requirements.txt --force-reinstall

# 查看详细错误日志
python main.py 2>&1 | tee error.log
```

### 问题 2: 前端无法连接后端

**症状**: 浏览器控制台显示 Network Error

**解决方案**:
```bash
# 1. 确认后端运行正常
curl http://localhost:8000/api/v1/health

# 2. 检查 CORS 配置
# 查看 edge/src/api_server.py 中的 CORS 设置

# 3. 确认环境变量正确
cat frontend/.env.development
# VITE_API_BASE_URL 应该是 http://localhost:8000/api/v1

# 4. 重启前端服务
cd frontend
npm run dev
```

### 问题 3: WebSocket 连接失败

**症状**: 不断重连或显示未连接

**解决方案**:
```bash
# 1. 检查 WebSocket 端点
curl -i -H "Upgrade: websocket" -H "Connection: Upgrade" ws://localhost:8000/ws

# 2. 查看防火墙设置
# Windows: 允许 8000 端口
netsh advfirewall firewall add rule name="AI Inspection" dir=in action=allow protocol=TCP localport=8000

# 3. 检查后端日志
# 查找 WebSocket 连接相关日志
```

### 问题 4: 摄像头无法打开

**症状**: 提示"无法打开摄像头"或黑屏

**解决方案**:
```bash
# 1. 检查摄像头是否被其他程序占用
# 关闭可能使用摄像头的程序（微信、Zoom 等）

# 2. 测试摄像头
python -c "import cv2; cap = cv2.VideoCapture(0); ret, frame = cap.read(); print('摄像头正常' if ret else '摄像头异常')"

# 3. 修改配置
# 编辑 edge/config/config.yaml
# 尝试不同的 source 值（0, 1, 2 或 RTSP URL）

# 4. 使用 IP 摄像头
# 确保网络连通性
ping <camera-ip>
```

### 问题 5: 检测结果不更新

**症状**: 统计数据不变或 UI 不更新

**解决方案**:
```bash
# 1. 确认主循环正在运行
# 查看后端日志中的检测帧率

# 2. 检查 WebSocket 广播
# 在浏览器控制台查看是否收到消息

# 3. 清除浏览器缓存并刷新
Ctrl + F5

# 4. 重启服务
# 先后端、再前端
```

### 问题 6: 内存占用过高

**症状**: 内存使用率持续增长

**解决方案**:
```bash
# 1. 启用自动垃圾回收
# 系统已内置性能优化器，会自动管理内存

# 2. 手动触发垃圾回收
import gc
gc.collect()

# 3. 调整缓存策略
# 编辑 edge/config/config.yaml
cache:
  max_size: 1000  # 减少缓存数量
  ttl: 300  # 缩短缓存时间
```

---

## 性能优化

### 1. GPU 加速

```bash
# 安装 GPU 版本的 ultralytics
pip uninstall ultralytics
pip install ultralytics[gpu]

# 验证 GPU 是否启用
python -c "import torch; print('GPU:', torch.cuda.is_available())"
```

### 2. 模型优化

```bash
# 使用更小的模型（速度更快，精度略低）
# 编辑 edge/config/config.yaml
model:
  path: yolov8n.pt  # nano 版本
  
# 或使用量化模型
# yolov8n_int8.pt (INT8 量化)
# yolov8n_fp16.pt (FP16 量化)
```

### 3. 检测参数优化

```yaml
# 编辑 edge/config/config.yaml
detection:
  confidence_threshold: 0.45  # 降低阈值（更多检测）
  iou_threshold: 0.5  # 提高阈值（更少重复）
  max_detection_per_frame: 5  # 限制最大检测数
```

### 4. 缓存优化

```yaml
# 编辑 edge/config/config.yaml
cache:
  enabled: true
  max_size: 2000  # 最大缓存数
  ttl: 600  # 缓存过期时间（秒）
```

---

## 备份恢复

### 数据备份

```bash
# 备份配置文件
xcopy /E /I edge\config config_backup_%date:~0,4%%date:~5,2%%date:~8,2%
xcopy /E /I frontend\.env env_backup_%date:~0,4%%date:~5,2%%date:~8,2%

# 备份模型文件
xcopy /E /I edge\models models_backup_%date:~0,4%%date:~5,2%%date:~8,2%

# 备份检测报告
xcopy /E /I edge\detection_results results_backup_%date:~0,4%%date:~5,2%%date:~8,2%
```

### 数据恢复

```bash
# 恢复配置文件
xcopy /E /I config_backup_* edge\config

# 恢复模型文件
xcopy /E /I models_backup_* edge\models
```

### 日志清理

```bash
# 清理旧日志（保留最近 7 天）
forfiles /p edge\logs /s /m *.log /d -7 /c "cmd /c del @path"
forfiles /p frontend\logs /s /m *.log /d -7 /c "cmd /c del @path"
```

---

## 日常运维

### 启动检查清单

- [ ] 后端服务运行正常
- [ ] 前端服务运行正常
- [ ] WebSocket 连接正常
- [ ] 摄像头工作正常
- [ ] 模型加载成功
- [ ] CPU/内存使用率正常
- [ ] 磁盘空间充足

### 定期维护

**每日**:
- [ ] 检查系统日志
- [ ] 查看健康报告
- [ ] 清理临时文件

**每周**:
- [ ] 运行可靠性测试
- [ ] 备份重要数据
- [ ] 检查摄像头清洁度

**每月**:
- [ ] 更新系统补丁
- [ ] 评估性能指标
- [ ] 优化配置参数

---

## 技术支持

### 日志位置
- **后端日志**: `edge/logs/`
- **前端日志**: `frontend/logs/`
- **测试日志**: `tests/*.log`
- **健康报告**: `tests/health_report_*.json`
- **可靠性报告**: `tests/reliability_report_*.json`

### 有用的命令

```bash
# 查看实时日志
tail -f edge/logs/app.log

# 查看错误日志
grep "ERROR" edge/logs/app.log

# 监控系统资源
python tests\system_health_monitor.py --interval 5

# 导出配置
python -c "import yaml; print(yaml.dump(open('edge/config/config.yaml').read()))"
```

---

## 附录

### A. 端口使用
- **8000**: API 服务器 + WebSocket
- **5173**: 前端开发服务器
- **1883**: MQTT Broker（可选）

### B. 文件结构
```
AISJZJRJT/
├── edge/              # 后端服务
│   ├── src/          # 源代码
│   ├── config/       # 配置文件
│   ├── logs/         # 日志文件
│   └── models/       # 模型文件
├── frontend/         # 前端服务
│   ├── src/         # 源代码
│   ├── public/      # 静态资源
│   └── .env.*       # 环境配置
├── tests/           # 测试脚本
├── docs/            # 文档
└── config/          # 全局配置
```

### C. 环境变量
```bash
# 后端
API_KEY=your-api-key
LOG_LEVEL=INFO

# 前端
NODE_ENV=development/production
```

---

**版本**: 1.0  
**更新日期**: 2026-03-23  
**维护者**: AI视觉质检系统团队
