# 网络摄像头功能 - 快速启动指南

## 🚀 立即使用

### 1. 访问页面
打开浏览器访问：
```
http://localhost:3001/network-cameras
```

### 2. 扫描摄像头
1. 点击 **"扫描网络摄像头"** 按钮
2. 输入 IP 范围（例如：`192.168.1.1-192.168.1.255`）
3. 点击 **"开始扫描"**
4. 等待扫描完成（约 1-3 分钟）

### 3. 添加摄像头
1. 在扫描结果中找到可用的摄像头
2. 点击 **"添加"** 按钮
3. 填写摄像头信息：
   - ID: `camera_003`（自动填充）
   - 名称：`门口摄像头`
   - RTSP 地址：自动生成
4. 点击 **"确定添加"**

### 4. 测试连接
- 点击 **"测试连接"** 验证摄像头是否可用
- ✅ 绿色 = 在线
- ❌ 红色 = 离线/故障

## 📋 常用 IP 范围参考

| 网络环境 | IP 范围示例 |
|---------|------------|
| 家庭网络 | `192.168.1.1-192.168.1.255` |
| 办公室网络 | `192.168.0.1-192.168.0.255` |
| 工厂网络 | `10.0.0.1-10.0.0.255` |

**查看本机 IP**（确定网段）：
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
```

## 🔧 常见问题

### Q1: 扫描不到摄像头？
**检查清单**：
- [ ] 确认摄像头已通电并连接到网络
- [ ] 确认 IP 范围正确（与摄像头在同一网段）
- [ ] 关闭防火墙或添加例外规则
- [ ] 尝试更大的超时时间（5-10 秒）

### Q2: 无法添加摄像头？
**可能原因**：
- RTSP 地址格式错误
- 摄像头需要认证
- 网络不通

**解决方法**：
1. 使用 VLC 播放器测试 RTSP 地址
2. 在地址中添加用户名密码：
   ```
   rtsp://username:password@ip:port/stream
   ```

### Q3: 页面打不开？
**检查步骤**：
1. 确认前端服务正在运行：
   ```bash
   # 应该看到 Vite 运行中
   npm run dev
   ```
2. 检查端口是否正确（默认 3001）
3. 清除浏览器缓存后重试

## 🎯 API 测试（可选）

### 使用 curl 测试
```bash
# 1. 扫描摄像头
curl -X POST "http://localhost:8000/api/v1/cameras/network/scan" \
  -H "Content-Type: application/json" \
  -d '{"ip_range": "192.168.1.1-192.168.1.255", "timeout": 2.0}'

# 2. 获取结果
curl "http://localhost:8000/api/v1/cameras/network/scan-results"

# 3. 添加摄像头
curl -X POST "http://localhost:8000/api/v1/cameras/network/add" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "camera_003",
    "name": "测试摄像头",
    "rtsp_url": "rtsp://192.168.1.100:554/stream1"
  }'
```

## 📊 支持的摄像头品牌

| 品牌 | 默认端口 | URL 格式 |
|------|---------|---------|
| 海康威视 | 554 | `rtsp://ip:554/h264/ch1/main/av_stream` |
| 大华 | 554 | `rtsp://ip:554/cam/realmonitor?channel=1&subtype=0` |
| Axis | 554 | `rtsp://ip:554/live/ch1` |
| 华为 | 554 | `rtsp://ip:554/streaming/channels/101` |
| 其他 | 554/8554 | `rtsp://ip:554/stream1` |

## 💡 优化建议

### 扫描优化
- **小范围扫描**：先扫描少量 IP（如 `.1-.50`），确认成功后再扩大范围
- **避开高峰**：在网络空闲时段扫描
- **调整超时**：网络差时增加超时时间

### 性能优化
- **限制数量**：建议同时运行的摄像头不超过 8 个
- **降低分辨率**：使用 720p 代替 1080p
- **合理帧率**：25-30 FPS 足够大多数场景

## 🔒 安全提醒

1. **修改默认密码**：所有摄像头必须修改出厂默认密码
2. **启用认证**：RTSP 流应配置用户名密码
3. **网络隔离**：摄像头放在独立 VLAN
4. **定期更新**：及时升级摄像头固件

## 📞 需要帮助？

详细文档：`docs/网络摄像头自动添加指南.md`  
功能说明：`NETWORK_CAMERA_FEATURE.md`

---

**祝使用愉快！** 🎉
