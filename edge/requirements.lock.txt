# ============================================================================
# AI 视觉质检系统 (AIQC) — Python 依赖锁定文件 (requirements.lock.txt)
# ----------------------------------------------------------------------------
# 生成环境 : Python 3.13.14 (venv: .workbuddy/binaries/python/envs/aiqc), Windows
# 生成命令 : pip freeze > requirements.lock.txt
# 用途     : 生产 / CI 可复现构建。Dockerfile.backend 默认从此文件安装依赖。
# 跨平台   : 本文件为「版本锁定」而非「平台锁定」——pip 会按目标平台拉取
#            对应的 wheel（manylinux / win_amd64 等），因此在 Linux 容器与
#            Windows 开发机上安装的是同一组版本，可复现。
# 惰性可选 : ultralytics / opencv-python-headless 是「真实 YOLO 推理 / 真实摄像头
#            采集」的惰性依赖（缺失时自动降级为仿真）。当前锁定集未包含
#            ultralytics（开发环境未安装）。若需在容器中启用真实推理，构建时传
#            --build-arg REAL_INFERENCE=1（Dockerfile 会按需安装二者）。
# 版本策略 : requirements.txt 为「意图清单」（宽松范围，便于开发演进）；本文件为
#            「落地快照」（精确版本，保证构建可复现）。二者应同步维护。
# ============================================================================

annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
colorama==0.4.6
et_xmlfile==2.0.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
iniconfig==2.3.0
numpy==2.5.2
opencv-python-headless==5.0.0.93
openpyxl==3.1.5
packaging==26.3
pluggy==1.6.0
psutil==7.2.2
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
PyJWT==2.13.0
pytest==9.1.1
python-dotenv==1.2.3
python-multipart==0.0.32
PyYAML==6.0.3
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.52.4
watchfiles==1.2.0
websockets==17.0.1
