# 后端运行依赖（Python 3.9+）
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5
psutil>=5.9
pyjwt>=2.8
openpyxl>=3.1
python-multipart>=0.0.9
# 真实摄像头采集（OpenCV）：惰性导入，未安装时自动降级为仿真，非必须
opencv-python-headless>=4.9
# 真实 YOLO 推理（启用模型权重时）：惰性导入，未安装时自动降级为仿真。会按需拉取 torch(CPU)
ultralytics>=8.0
