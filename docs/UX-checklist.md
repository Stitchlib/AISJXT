# UX 走查清单（阶段三 · 3.4 交互细节补齐）

> 生成日期：2026-08-26
> 用途：阶段三验收证据。逐页走查 10 个页面，确认「全局错误规范 / 空态与加载态 / 导出防重复 / 角色可见性 / 断网自愈」均已落地。
> 走查方式：本地 `npm run dev` + 后端联调；标注 ✅ 已通过 / ⚠️ 已知取舍 / ❌ 未覆盖（本版无 ❌）。

---

## 一、全局规范（跨页统一）

| 项 | 实现位置 | 状态 |
|----|----------|------|
| 401 自动刷新并重放；刷新失败回落登录页 | `src/api/client.js` 响应拦截器 | ✅ |
| 403 文案「权限不足」；5xx 文案「服务异常」 | `src/api/client.js` | ✅ |
| ElMessage 错误 toast 统一，不重复弹 | `client.js` `redirecting` 去重 | ✅ |
| loading 态：列表/提交统一 `v-loading` | 各 views | ✅ |
| 表格空态：无数据时 `el-empty` 占位 | 各列表页 | ✅ |
| 导出按钮防重复点击（disabled + loading） | 报表/历史页导出按钮 | ✅ |
| 角色可见性：侧边栏按 `store.user.role` 过滤 | `router/index.js` + 菜单渲染 | ✅ |
| 越权直达 URL → 跳 403 页 | `router.beforeEach` | ✅ |
| 断网重连指数退避 + 可见性立即重试 + 重连后状态对齐 | `src/utils/websocket.js` | ✅ |
| 移动端最小适配：≥1280 优先，关键页响应式不破版 | 布局栅格 | ✅（已知取舍：未做 <1024 深度适配） |

---

## 二、逐页走查（10 页）

### 1. 登录 `Login.vue`
- [x] 登录失败 toast 文案明确（账号/密码错误）；不回显明文密码。
- [x] 登录成功写入 token + user（含 role），跳转仪表盘。
- [x] 401 不触发全局刷新逻辑（登录接口自身 401 直接交调用方）。

### 2. 系统仪表盘 `SystemDashboard.vue`
- [x] 加载态骨架；资源卡片（CPU/内存/磁盘）+ **L9 业务指标卡片**（推理延迟 P50/P95、丢帧率、WS 连接数、DB 大小、写入 QPS、检测器模式、配置状态）。
- [x] 数据为空时显示占位而非空白。

### 3. 实时质检 `RealTimeInspection.vue`
- [x] 视频流经 **stream-ticket**（`?ticket=`）加载，不暴露 `?token=`；取票失败有错误态。
- [x] 控制按钮（start/stop）按角色/连接状态禁用；viewer 收到控制拒绝有提示。
- [x] 刷新画面按钮防抖；加载态占位。

### 4. 设备管理 `DeviceManagement.vue`
- [x] 摄像头列表空态；缩略图 10s 自动换票刷新。
- [x] 添加/编辑对话框：实时预览（preview ticket）加载态与错误态。
- [x] 启停操作成功/失败 toast；越权操作（viewer）被后端 403 拦截并提示。

### 5. 模型监控 `ModelMonitoring.vue`
- [x] 版本列表空态；激活/回滚操作二次确认。
- [x] 上传进度与失败提示；坏权重激活被后端 400 拒绝且 active 不变。

### 6. 质检报告 `QualityReport.vue`
- [x] 筛选加载态；无结果空态。
- [x] 导出 CSV 按钮防重复点击；导出失败 toast。

### 7. 历史记录 `HistoryRecords.vue`
- [x] 分页加载态；空态占位。
- [x] 批量导出防重复点击；导出中禁用按钮。

### 8. 系统配置 `SystemConfig.vue`
- [x] **仅 admin 可见可达**（路由 meta roles + 守卫）；viewer 直达跳 403。
- [x] 敏感字段（smtp_pass）回显 `***`；提交空密码不覆盖。
- [x] 保存成功/失败 toast；配置损坏时 health 标记 `config_degraded`。

### 9. 告警中心 `Alerts.vue`
- [x] 未读/已读状态区分；加载态与空态。
- [x] 标记已读操作反馈 toast。

### 10. 用户管理 `UserManagement.vue`
- [x] **仅 admin 可见可达**；viewer 跳 403。
- [x] 列表空态；新增/编辑/删除二次确认。
- [x] **修改密码**对话框：本人改验旧口令、admin 重置免验；失败 toast。
- [x] **审计流水**标签页：admin 专属，展示 who/what/when/ip。

---

## 三、结论

阶段三 3.4 交互细节补齐 **全部通过**：全局错误规范、空态/加载态、导出防重复、角色可见性、断网自愈均已落地并经 10 页走查验证。唯一已知取舍为移动端仅保证 ≥1280 优先（计划明确「≥1280 优先」），不深入 <1024 适配——符合计划约定，非缺陷。

> 注：token 存储仍为 localStorage（计划 L2 明确记录取舍，后续可升级 httpOnly cookie）；WS 鉴权沿用 `?token=` 握手（计划 1.2 验收口径），视频流已升级为一次性 ticket（M7）。
